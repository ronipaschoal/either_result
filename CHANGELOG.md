## 0.1.0

- Extracted `Result`/`ResultSuccess`/`ResultFailure` from the `insura` app
  (`lib/core/result/result.dart`) into a standalone package.
- Added `map`, `mapFailure`, `getOrElse`, `successOrNull` and `failureOrNull`.
